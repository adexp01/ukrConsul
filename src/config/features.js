/*
 * Прапорці розділів, які тимчасово не показуємо.
 *
 * false означає, що блоку немає по-справжньому: він не рендериться, у розмітці
 * сторінки його не знайти, маршрут не зареєстрований, у меню пункту немає.
 * Це навмисно замість закоментованої розмітки — закоментований код лишається
 * у бандлі й читається в DevTools, а тут у сторінку не потрапляє нічого.
 *
 * Прості константи, а не поля обʼєкта, — щоб збірник міг згорнути умову й
 * викинути з бандла самі компоненти разом із їхніми стилями.
 *
 * Щоб повернути розділ — поставити true. Більше нічого правити не потрібно.
 */

/** Сторінка «Заходи», пункт меню й блок «Найближчі події» */
export const EVENTS_ENABLED = false;

/** Блок «Команда» на сторінці «Про нас» */
export const TEAM_ENABLED = false;

/**
 * Додаткові секції сторінки «Медіа»: медіаінфо, добірка матеріалів, головний
 * матеріал, «Для журналістів», завантаження медіакіту й блок запиту.
 * Чекають на матеріали.
 */
export const MEDIA_SECTIONS_ENABLED = false;
