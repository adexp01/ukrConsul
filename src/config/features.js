/*
 * Прапорці розділів, які тимчасово не показуємо.
 *
 * false означає, що блоку немає по-справжньому: він не рендериться, у розмітці
 * сторінки його не знайти, маршрут не зареєстрований, у меню пункту немає.
 * Це навмисно замість закоментованої розмітки — закоментований код лишається
 * у бандлі й читається в DevTools, а тут у сторінку не потрапляє нічого.
 *
 * Прості константи, а не поля обʼєкта, — щоб збірник міг згорнути умову й
 * викинути з бандла самі компоненти разом із їхніми стилями.
 *
 * Щоб повернути розділ — поставити true. Більше нічого правити не потрібно.
 */

/** Сторінка «Заходи», пункт меню й блок «Найближчі події» */
export const EVENTS_ENABLED = false;

/**
 * Входи на сторінку «Діяльність»: пункт меню з випадним списком напрямів і
 * кнопка «Більше про проєкт» на слайді ZBROYA EXPO в галереї на головній.
 *
 * Один прапорець на обидва входи — щоб розділ зникав і повертався цілком, а
 * не половинами. Сама сторінка живе за адресою /office незалежно від цього:
 * прямі посилання з релізів і матеріалів працюють завжди.
 *
 * Був вимкнений, поки в розділі бракувало матеріалів (порожні описи
 * партнерських програм). Тепер увімкнений назад.
 */
export const ACTIVITIES_ENABLED = true;

/** Блок «Команда» на сторінці «Про нас» */
export const TEAM_ENABLED = false;

/** Блок «Для журналістів» на сторінці «Медіа» */
export const MEDIA_JOURNALIST_ENABLED = true;

/**
 * Решта секцій сторінки «Медіа»: медіаінфо, добірка матеріалів, головний
 * матеріал, завантаження медіакіту й блок запиту. Чекають на матеріали.
 *
 * Окремо від «Для журналістів» саме тому, що той блок уже має і текст, і
 * адресу для звернень, а решта — ні. Одним прапорцем на всі шість це не
 * розділити: або показуєш порожні блоки, або ховаєш готовий.
 */
export const MEDIA_SECTIONS_ENABLED = false;
